#include "min_heap.h"
#include <stdlib.h>

struct MinHeap {
    // TODO: Define your min heap structure
};

MinHeap* min_heap_create(void) {
    return NULL;
}

void min_heap_insert(MinHeap *heap, int value) {
    (void)heap;
    (void)value;
}

int min_heap_extract_min(MinHeap *heap) {
    (void)heap;
    return -1;
}

int min_heap_peek_min(const MinHeap *heap) {
    (void)heap;
    return -1;
}

bool min_heap_is_empty(const MinHeap *heap) {
    (void)heap;
    return true;
}

size_t min_heap_size(const MinHeap *heap) {
    (void)heap;
    return 0;
}

void min_heap_destroy(MinHeap *heap) {
    (void)heap;
}
